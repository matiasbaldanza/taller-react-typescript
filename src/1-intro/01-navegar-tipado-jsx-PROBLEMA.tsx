export const Componente = () => {
  return (
    <div                  // control-click on me to go to the div types
      aria-posinset={ }   // hover para ver la definición.
      onChange={ } // cómo sé que tipo expera onChange? 

    // Para tener autocompletado en JSX: ctrl-space
    />
  );
};